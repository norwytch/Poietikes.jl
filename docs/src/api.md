# API reference

Everything `Poietikes` exports (plus its documented internals). The user-facing entry points are
[`analyze`](@ref), [`detect_language`](@ref) / [`detect_form`](@ref), [`scansion`](@ref), and the
[`@form`](@ref) / [`load_forms`](@ref) extension path.

```@autodocs
Modules = [Poietikes]
Order   = [:module, :type, :function, :macro]
```
